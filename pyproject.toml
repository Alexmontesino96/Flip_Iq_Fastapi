[project]
name = "flipiq"
version = "0.1.0"
description = "SaaS para resellers - evalúa productos antes de comprarlos para revender"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "integration: tests that require network access",
]
